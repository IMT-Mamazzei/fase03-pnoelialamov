package br.maua.cic303;

import java_cup.runtime.*;

parser code {:
    // Método chamado pelo CUP quando encontra um erro sintático
    public void syntax_error(Symbol cur_token) {
        throw new RuntimeException("Erro Sintático na linha " + cur_token.left + ", coluna " + cur_token.right);
    }
:}

/* ========================================================================= */
/* DECLARAÇÃO DOS TOKENS (TERMINAIS)                                         */
/* O CUP usará isso para gerar o sym.java que o seu Lexer precisa            */
/* ========================================================================= */

terminal IF, THEN, ELSE, WHILE;
terminal ASSIGN, LPAREN, RPAREN, LBRACE, RBRACE, SEMI;
terminal String ID, NUMBER;
terminal String ADD_OP, MUL_OP, REL_OP;

/* ========================================================================= */
/* DECLARAÇÃO DOS NÃO-TERMINAIS (VARIÁVEIS DA GRAMÁTICA)                     */
/* ========================================================================= */

non terminal program, stmt_list, stmt;
non terminal assign_stmt, if_stmt, while_stmt, block_stmt, null_stmt;
non terminal expr;

/* ========================================================================= */
/* PRECEDÊNCIA E ASSOCIATIVIDADE                                             */
/* ========================================================================= */

/* RESOLVIDO: O operador mais embaixo tem a MAIOR precedência! */
precedence left REL_OP;  // Menor precedência
precedence left ADD_OP;  // Precedência média (+, -)
precedence left MUL_OP;  // Maior precedência (*, /, %)

/* ========================================================================= */
/* REGRAS DA GRAMÁTICA                                                       */
/* ========================================================================= */

start with program;

/* RESOLVIDO: Um programa é uma lista de comandos */
program ::= stmt_list ;

/* RESOLVIDO: Lista recursiva à esquerda (permite lista vazia ou múltiplos comandos) */
stmt_list ::= stmt_list stmt 
            | /* vazio */ 
            ;

/* RESOLVIDO: Definição de comando unificado */
stmt ::= assign_stmt
       | if_stmt
       | while_stmt
       | block_stmt
       | null_stmt
       ;

/* Atribuição: ID = expressao ; */
assign_stmt ::= ID ASSIGN expr SEMI ;

/* RESOLVIDO: Estrutura If: if ( expressao ) then bloco [else bloco] */
/* Note que a especificação exige a palavra 'THEN' e obriga o uso de 'BLOCO' (block_stmt) */
if_stmt    ::= IF LPAREN expr RPAREN THEN block_stmt
             | IF LPAREN expr RPAREN THEN block_stmt ELSE block_stmt ;

/* RESOLVIDO: Estrutura While: while ( expressao ) bloco */
while_stmt ::= WHILE LPAREN expr RPAREN block_stmt ;

/* Bloco: { lista_de_comandos } */
block_stmt ::= LBRACE stmt_list RBRACE ;

/* Comando nulo: ; */
null_stmt  ::= SEMI ;

/* RESOLVIDO: Expressões Matemáticas, Relacionais e identificadores */
expr ::= expr ADD_OP expr
<<<<<<< HEAD:fase03-o-problema-da-ordem/src/main/cup/Parser.cup
       | expr MUL_OP expr
       | expr REL_OP expr
       | LPAREN expr RPAREN
       | NUMBER
       | ID
       ;
=======
       /* Adicione MUL_OP, REL_OP, parenteses, NUMBER e ID */
       ;
>>>>>>> 23221096919fe05e3d65f48b39afdfe3e4d64aa4:fase03-o-problema-da-ordem/src/main/cup/parser.cup
